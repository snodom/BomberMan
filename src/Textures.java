/*public class Textures{

    private int x, y;
    public int size;
    private Image sheet;
    public int[] pixels;

    public static Textures player_forward_1 = new Textures(64, 0, 1, Image.tiles);
    public static Textures player_back_1 = new Textures(64, 0, 2, Image.tiles);
    public static Textures player_side_right = new Textures(64, 0, 3, Image.tiles);
    public static Textures player_side_left = new Textures(64, 1, 3, Image.tiles);


    public Textures(int size, int x, int y, Image tiles) {
        this.size = size;
        this.x = x * size;
        this.y = y * size;
        this.sheet = tiles;
        pixels = new int[size * size];
        tiles.load();
    }
}
*/